rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Property images: owner can write, public can read
    match /properties/{userId}/{propertyId}/{fileName} {
      allow read: if true;
      allow write: if request.auth != null
                   && request.auth.uid == userId
                   && request.resource.size < 10 * 1024 * 1024   // 10 MB limit
                   && request.resource.contentType.matches('image/.*');
    }

    // User profile assets (avatars, KYC docs, etc.)
    match /users/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    // KYC documents — owner can write, only authenticated users can read
    match /kyc/{userId}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
